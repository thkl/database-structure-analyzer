# Database Configuration
# Copy this file to .env and update with your database credentials

# Database Connection
DB_DIALECT=mysql
# Supported dialects: mysql, postgres, sqlite, mariadb, mssql

# MySQL/MariaDB Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=your_database_name
DB_USER=your_username
DB_PASSWORD=your_password

# PostgreSQL Configuration (if using postgres)
# DB_DIALECT=postgres
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=your_database_name
# DB_USER=your_username
# DB_PASSWORD=your_password

# SQLite Configuration (if using sqlite)
# DB_DIALECT=sqlite
# DB_NAME=./database.sqlite

# MSSQL/SQL Server Configuration (if using mssql)
# DB_DIALECT=mssql
# DB_HOST=localhost
# DB_PORT=1433
# DB_NAME=your_database_name
# DB_USER=your_username
# DB_PASSWORD=your_password
# DB_INSTANCE_NAME=SQLEXPRESS
# DB_ENCRYPT=true
# DB_TRUST_SERVER_CERTIFICATE=true

# SSL Configuration (optional, not for MSSQL)
# DB_SSL=true

# Output Configuration
OUTPUT_DIR=./output

# Debug Options
# DEBUG_PATHS=true  # Show pathfinding waypoints and buffer zones in SVG diagrams

# SVG Diagram Spacing Configuration (all values in pixels)
# Adjust these values to fine-tune the appearance and routing of relationship lines

# Canvas and Layout
# SVG_CANVAS_MARGIN=100      # Large margin around entire canvas for safe routing
# SVG_CONNECTION_MARGIN=25   # Space before lines can turn from table edges
# SVG_MAX_COLUMNS_TO_SHOW=40 # max number of columns in a table to show in the svg
# Collision and Visual Buffers
# SVG_COLLISION_BUFFER=25    # Minimum safety distance for collision detection
# SVG_VISUAL_BUFFER=50       # Additional spacing for professional appearance  

# Safe Routing Zones
# SVG_SAFE_ZONE_OFFSET=40    # How much to offset from canvas margin for safe routing
# SVG_ROUTING_SPACING_TOP=60 # Extra spacing from top edge for routing
# SVG_ROUTING_SPACING_SIDE=40 # Extra spacing from side edges for routing

# Example configurations:
# Tight spacing (for small diagrams):
# SVG_CANVAS_MARGIN=60 
# SVG_CONNECTION_MARGIN=15 
# SVG_COLLISION_BUFFER=15 
# SVG_VISUAL_BUFFER=25

# Generous spacing (for large complex diagrams):  
# SVG_CANVAS_MARGIN=150 
# SVG_CONNECTION_MARGIN=35 
# SVG_COLLISION_BUFFER=35 
# SVG_VISUAL_BUFFER=70